import totalOutputData from '../data/Total-Output-Data.json';

const processTotalOutputData = () => {
  const jobs = totalOutputData;
  return jobs;
  // TODO: Process your jobs data
  // e.g. console.log('jobs', jobs);
};

processTotalOutputData();
